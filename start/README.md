# Starting state for session
You can use this folder to start working on the steps.

## Initial steps
Execute the following commands to get the dependencies before startng the steps
```bash
cd start
npm install
```